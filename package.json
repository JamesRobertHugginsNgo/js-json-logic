{
  "name": "js-json-logic",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --build",
    "watch": "tsc --watch",
    "parcel:build": "parcel build src/parcel/index.html --no-optimize --dist-dir dist/parcel --public-url ./",
    "parcel:watch": "parcel src/parcel/index.html --dist-dir dist/parcel"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "@types/node": "^25.0.3",
    "parcel": "^2.16.3",
    "typescript": "^5.9.3"
  }
}
